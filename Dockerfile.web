# Serves a pre-built Flutter web bundle (no Flutter SDK on the droplet).
# On your PC: run scripts/build_web_prod.ps1 (or flutter build web with --dart-define below), then commit build/web and deploy.
FROM nginx:alpine
COPY build/web /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
