# Copy to .env on the droplet (and locally if needed). Never commit .env.
# Used by docker-compose for MariaDB root and matching API / phpMyAdmin credentials.

MYSQL_ROOT_PASSWORD=change_me_to_a_strong_password
